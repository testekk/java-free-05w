package lesson01;

/**
 * Các bạn sẽ phát triển code của các bạn ở đây, lưu ý mỗi người tạo 01 file và đặt tên theo rule
 * 1. Copy file này, và change name của file theo cú pháp sau: TrangTran_VariableExercise.java
 *  Ví dụ: Nguyễn Văn An -> NguyenVanAn_VariableExercise.java
 * 2. Truy cập file gitCommitMac.sh hoặc gitCommitWin.bat (Lựa chọn theo HĐH bạn đang sử dụng) và thêm thông tin commit của bạn.
 *   2.1 Tại dòng 8: Thay đường dẫn file của bạn tại [Your file Path] (Ví dụ: src/lesson01/NguyenVanAn_VariableExercise.java)
 *   2.2 Truy cập terminal có sử dụng IntelliJ IDEA: Vào View -> Tool Windows -> Terminal và thực thi dòng lệnh sau:
 *    - Trên Mac: sh gitCommitMac.sh
 *    - Trên Windows: gitCommitWin.bat
 *    2.3 Nếu gặp lỗi về quyền truy cập thì bạn cần cấp quyền cho file .sh hoặc .bat đó, mở terminal và thực thi lệnh sau:
 *    - Trên Mac: chmod +x gitCommitMac.sh
 *    - Trên Windows: Không cần thực thi lệnh này
 *  3. Sau khi thực thi xong, bạn sẽ thấy thông báo "Git commit successfully" trên terminal
 */
public class TrangTran_VariableExercise {
    public static void main(String[] args) {
        //region Bài 01
/*
		//Khai báo và khởi tạo các biến
		  int a = 30;
		  int b = 35;
		  double c = 3.5;
		 //Thực hiện các phép toán
		  double avg = (double)(a+b)/2; // Phép tb cộng
		  int sum = a+b; // Phép cộng
		  int difference = b-a; // Phép trừ
		  int product = a*b; // Phép nhân
		  double sum1= a+b+c; // Phép cộng
		  boolean isGreater = (a > b);
		  boolean isEqual= (a==b);

		  // in kết quả
		  System.out.println("trung binh cong" +" "+ avg);
		  System.out.println("tong" +" "+sum);
		  System.out.println("hieu" +" "+difference);
		  System.out.println("tich" +" "+product);
		  System.out.println("tong abc" +" "+sum1);
		  System.out.println(isGreater);
		  System.out.println(isEqual);


      */



        //endregion
/*
        //region Bài 02

		//Khai báo và khởi tạo các biến
        int num1 = 10;
        int num2 = 20;

        double real1 = 15.5;
        double real2 = 30.7;

        boolean bool1 = true;
        boolean bool2 = false;

        String str1 = "Hello";
        String str2 = "World";

        // Thực hiện các phép toán so sánh với các biến số nguyên và số thực
        boolean isEqualInt = (num1 == num2);  // So sánh số nguyên
        boolean isGreaterReal = (real1 > real2);  // So sánh số thực

        // Thực hiện các phép toán logic với các biến boolean
        boolean andResult = bool1 && bool2;   // Phép logic AND
        boolean orResult = bool1 || bool2;    // Phép logic OR

        // Thực hiện phép toán cộng chuỗi với biến kiểu String
        String combinedString = str1 + " " + str2;  // Cộng chuỗi

        // In kết quả ra màn hình
        System.out.println("Kết quả so sánh số nguyên: num1 == num2? " + isEqualInt);
        System.out.println("Kết quả so sánh số thực: real1 > real2? " + isGreaterReal);

        System.out.println("Kết quả phép toán logic AND: " + andResult);
        System.out.println("Kết quả phép toán logic OR: " + orResult);

        System.out.println("Kết quả cộng chuỗi: " + combinedString);

*/

        //endregion
    }
}

